{
  "name": "antlr-odata",
  "repository": {
    "url": "git+https://github.com/cam-m/antlr-odata.git",
    "type": "git"
  },
  "homepage": "https://github.com/cam-m/antlr-odata-ts",
  "version": "1.0.4",
  "type": "module",
  "description": "Antlr 4 OData Typescript parser",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "clean": "rimraf dist",
    "build": "npm run clean && npm run antlr && npm run compile-src-dist",
    "test": "npm run build && npm run tests",
    "dev": "npm run antlr && concurrently --kill-others \"npm run compile-src-watch\" \"npm run tests-watch\"",
    "prepack": "npm run build",
    "compile-src-dist": "tsc -p tsconfig.dist.json",
    "compile-src": "tsc -p tsconfig.json",
    "compile-src-watch": "tsc -w -p tsconfig.json",
    "tests": "tsx --test",
    "tests-watch": "tsx --test --watch",
    "antlr": "antlr-ng -Dlanguage=TypeScript -l -v -o src src/grammar/OData4Lite.g4"
  },
  "files": [
    "dist/**/*"
  ],
  "keywords": [
    "odata",
    "odata4",
    "parser",
    "antlr",
    "antlr4",
    "typescript"
  ],
  "author": "Cameron Maunder",
  "license": "MIT",
  "dependencies": {
    "antlr4ng": "^3.0.16",
    "trie-prefix-tree": "^1.5.1",
    "xpath": "^0.0.34"
  },
  "devDependencies": {
    "@types/assert": "^1.5.6",
    "@types/node": "^26.4.1",
    "@types/trie-prefix-tree": "^1.5.0",
    "@types/xmldom": "^0.1.31",
    "@xmldom/xmldom": "^0.8.2",
    "antlr-ng": "^1.0.10",
    "assert": "^2.0.0",
    "concurrently": "^7.2.2",
    "npm-watch": "^0.11.0",
    "rimraf": "^3.0.2",
    "tsconfig-paths": "^4.2.0",
    "tslint": "^6.1.3",
    "tsx": "^4.20.5",
    "typescript": "^7.0.2",
    "vitest": "^5.0.0"
  }
}
